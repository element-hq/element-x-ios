# Progress Log

Detailed daily progress log moved from CLAUDE.md to reduce file size.

| Date | What was done |
|------|---------------|
| 2026-02-08 | Phase 0 complete: all documentation created, codebase audited, 12 decisions tracked |
| 2026-02-09 | Phase 1 partial: fork, build env, first build, change map, git structure, slash commands |
| 2026-02-10 | Build re-verified. Customer outreach docs finalized. Ready for customer engagement. |
| 2026-02-10 | Firebase FCM integration on `feature/firebase-fcm-integration`. Firebase SDK v11.8.x, FirebaseNotificationService, conditional APNs/FCM, placeholder GoogleService-Info.plist. Deployment target 18.0. |
| 2026-02-11 | Firebase FCM unit tests (14 tests, 3 files). Pre-rebranding audits (5 docs, 97 identifiers). Automation scripts: `rebrand.sh` (712 lines) + `rebrand_strings.sh` (560 lines). Additional audits (Element Call, privacy manifests, upstream sync). Privacy manifest gaps fixed. Upstream sync (18 commits, SDK v26.02.10). Build verified. |
| 2026-02-12 | App Store preparation templates: export compliance, privacy labels, review notes, age rating, 4.3 differentiation. |
| 2026-02-16 | Created 6 Claude Code commands. Updated permissions. Build re-verified. |
| 2026-02-17 | **Major unblock: customer responded.** 5 decisions resolved. Server connectivity verified (matrix.ucmeet.org). AppSettings updated. App icon replaced. OIDC login working (3 issues fixed). Calls configured (URL scheme, knownHosts, LiveKit). Localization trimmed 37→3 locales. Associated domains cleaned. String rebranding (30 replacements). Swift source cleanup (10 refs in 8 files). Unit test fixes (16 assertions in 5 files). Display name "UCMeet". Branding audit: zero user-visible Element branding. Checkpoint `branding-complete`. Handover guide written. |
| 2026-02-18 | Platform restriction (no Mac/Vision Pro). Push gateway → `push.ucmeet.org`. NSE notification ID → dynamic bundle ID. |
| 2026-02-20 | Customer "Forking Data.doc" applied. Bundle ID `org.ucmeet.UCMeetChat` cascaded through 22 files. Display name `UCMeet.Chat`. 962 tests: 899 passed, 0 new failures. |
| 2026-03-01 | Bundle ID casing fix. New logos (square + circular 3D). Accent color #003B5D. Sprint docs. D-008 resolved. |
| 2026-03-02 | Apple Developer Portal: Bundle IDs registered, App Group created, capabilities enabled. APNs key received (XZANH7CD3Z). Access issue: ASC Admin ≠ Developer Program member. |
| 2026-03-03 | Xcode signing resolved (customer's Apple ID). OIDC redirect → custom URL scheme `org.ucmeet.UCMeetChat:/callback`. Logo sizing fix (330×330px @3x). Real Firebase GoogleService-Info.plist. |
| 2026-03-07 | MapLibre API key configured in Secrets.swift. Styles set to `basic-v2`/`basic-v2-dark`. Interactive map works. Static map previews return 403 (key needs Static Maps API permission on MapTiler). |
| 2026-03-11 | APNs key uploaded to Firebase Console. Firebase service account JSON generated. Customer appeal sent (`sprint3_customer_appeal.md`) with detailed Sygnal config: both app IDs (`.ios.dev` + `.ios.prod`), `fcm_v1` type, project ID `matrix-8c24a`. All developer-side push work complete. Upstream divergence noted: 46 ahead / 92 behind — sync planned before TestFlight. CLAUDE.md compacted from 42.4k to ~7k chars. |
| 2026-03-13 | **Upstream sync + Sprint 5 started.** Merged 89 upstream commits (SDK v26.03.10, Element Call v0.17.0). Resolved 79 conflicts (68 locale deletes, 5 Russian strings, AppSettings.swift, 5 test files). Sprint 5 release prep: version set to 1.0.0 (Build 1), Info.plist permissions verified (en + ru), debug log audit clean, iOS 18 minimum confirmed, NOTICE correct. Remaining: Distribution signing (Archive), encryption declaration decision, AGPL confirmation from customer. Build verified on iPhone 17 Pro. |
| 2026-03-17 | **Push E2E testing + NSE fix + upstream sync.** NSE entitlement `com.apple.developer.usernotifications.filtering` removed (requires Apple approval, blocked real-device builds). Temporary debug logging added for FCM token and pusher details. Push E2E test on real device: FCM pusher registration succeeds, gateway `https://push.ucmeet.org` (ntfy) reachable, but ntfy rejects FCM token (`"rejected"` response) — blocked on customer checking ntfy server logs. Second upstream sync: merged 13 commits (translations, compound-design-tokens v6/v7, Classic accounts, key backup fix). Team ID corrected in docs: `26UC01GH` → `6HRG779SDK`. CLAUDE.md and decisions_tracker.md updated with current status. Build verified on iPhone 17 Pro simulator. |
| 2026-03-18 | **Push E2E test #2 + Classic app group fix + Archive build + encryption declaration.** Removed upstream's `group.im.vector` App Group (Element Classic accounts feature) — blocked Xcode signing. Full E2E push test: simulator → real device, app-side confirmed working, server-side break identified (Synapse → ntfy). Analyzed customer ntfy logs, drafted diagnostic message. `ITSAppUsesNonExemptEncryption` changed to `true` (Matrix E2EE via Olm/Megolm qualifies for mass market exemption EAR §740.17(b)(1)). **Archive build succeeded** — distribution signing works with customer's Apple ID. Sprint 5 tasks updated. |
| 2026-03-18 | **TestFlight upload + App Store Connect + customer guide.** Removed `ITSAppUsesNonExemptEncryption` from Info.plist and target.yml — caused export compliance code mismatch during upload. Encryption questions now handled interactively in Xcode upload wizard. App created in App Store Connect (Apple ID: 6759875787, SKU: ucmeetchat, categories: Social Networking + Productivity). **Build uploaded to TestFlight successfully.** Created Russian-language App Store publication guide for customer (MD + HTML + PDF) — covers descriptions, screenshots, URLs, legal, 20-item checklist. Sprint 5 marked DONE, Sprint 6 started. |
| 2026-03-22 | **Build 2 + Apple fixes + customer URLs.** Updated all `policy-152` URLs to customer's confirmed pages: privacy→`privacy-security`, terms→`ucmeetchat-terms-of-use`, support→`support`. Removed invalid `NSPrivacyAccessedAPICategoryNetworkInformation` from app and NSE privacy manifests (ITMS-91054 rejection). Removed temporary FCM debug logging. Bumped build number 1→2. Uploaded encryption compliance document to App Store Connect (under review). Customer added as external TestFlight tester. Subtitle "Безопасный мессенджер на протоколе Matrix" exceeds 30-char limit — shortened to "Мессенджер на протоколе Matrix" (exactly 30 chars). |
| 2026-03-22 | **App Store Connect listing filled.** All ASC fields completed from customer-confirmed guide: RU+EN descriptions, promotional text, keywords, copyright (© 2026 UC.Technology LLC), review credentials (apple_support), review notes, contact phone (+7 495-108-69-15). URLs fixed to HTTPS (support, marketing, privacy). Subtitle set: "Мессенджер на протоколе Matrix". Age rating set to 14+. Regions: all countries. Re-added `ITSAppUsesNonExemptEncryption=YES` to plist+target.yml. Version decision: release as 1.0.0 (Build 2), future updates start from 1.0.1. **Developer-side work complete — all remaining items on customer side:** screenshots from TestFlight, Privacy Nutrition Labels, review contact name/email, ntfy push fix, AGPL confirmation. |
| 2026-03-24 | **Build 3 prep: 6 customer-reported issues fixed.** (1) OIDC dialog: `APP_NAME: ElementX` → `UCMeet.Chat` in project.yml — iOS system dialog now shows correct app name. (2) Analytics disabled: PostHog host/key + Sentry DSNs set to `nil` — analytics prompt and settings row hidden. (3) Bug reports disabled: rageshake URL set to `nil` — "Report a problem" hidden. (4) MapTiler key `iKPA4bK9zgtadTEw8neu` configured — interactive maps and location sharing work; static map previews show "Invalid key" (free plan doesn't include Static Maps API, customer deciding on paid upgrade). (5) Russian translations: 13 new keys added + 1 fixed (`screen_roomlist_your_spaces` "Your spaces"→"Ваши пространства"). (6) Green→navy blue: 20 SwiftUI CompoundColors + 19 UIKit CompoundUIColors overrides in CompoundHook.swift — toggles, icons, text, badges, gradient all navy blue #003B5D. Customer switching from ntfy to Sygnal for push — all credentials provided to customer's developer. Build verified on iPhone 17 Pro simulator. |
| 2026-03-24 | **Test infrastructure fix.** PRODUCT_MODULE_NAME: ElementX added to main target (keeps @testable import working after APP_NAME rename). TEST_HOST overridden in UnitTests/PreviewTests/IntegrationTests to use $(APP_NAME). 927 tests run, 861 passed, 66 pre-existing failures, 0 new. |
| 2026-03-25 | **Push notification debugging + Build 4 uploaded.** Extensive E2E push testing with customer. Diagnosed 3 successive issues: (1) Sygnal app IDs were placeholders `com.example.myapp.*` → fixed to `org.ucmeet.UCMeetChat.ios.prod/dev`. (2) Legacy FCM API permanently shut down by Google June 2024 → advised customer to use FCM v1 with service account JSON. (3) Sygnal iOS config was `type: apns` but app sends FCM tokens → must be `type: gcm`. Synapse logs confirmed: `500 Internal Server Error` when Sygnal tried to send FCM token to APNs. Root cause identified, fix provided to customer. Build 4 uploaded to TestFlight by developer. Next: Privacy Nutrition Labels. |
| 2026-03-26 | **Push: Firebase → APNs switch.** Customer tested with `type: gcm` — Sygnal now reached FCM but got 400: "Invalid JSON payload — Unknown name alert/mutable-content". Root cause: Sygnal's GCM pushkin puts APNs-format default_payload into FCM `data` field, but FCM v1 only accepts flat strings. Solution: switched app push provider from `.firebase` to `.apns` in AppSettings.swift. App now registers APNs device token directly. Sygnal must use `type: apns` with .p8 key. Firebase SDK stays in project. Customer also reported 4 new issues: (1) app crashes — fixed in Build 2 (Firebase re-init guard). (2) Maps on web — customer-side Element Web config. (3) Untranslated strings — 4 more Russian translations added. (4) Green send button — `gradientActionStop1-4` overridden to navy blue. Customer also requested matrix.to → ucmatrix.org change — pending confirmation. |
