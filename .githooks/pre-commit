#!/bin/bash

export PATH="$PATH:/opt/homebrew/bin"

if ! swiftformat --lint . > /dev/null 2>&1
then
  echo "pre-commit: Commit aborted to run SwiftFormat, please try again"
  swiftformat . > /dev/null 2>&1
  exit 1
fi