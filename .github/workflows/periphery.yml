name: Unused code

on:
  pull_request:
  push:
    branches: ["develop"]

jobs:
  periphery:
    permissions:
      actions: read
      contents: read
      id-token: write
    uses: periphery-pro/actions/.github/workflows/scan.yml@v1
    with:
      runner: macos-26
      setup: git lfs pull
